package TestNgPageRepository;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class LoginPage {
	
	
/*	WebDriver driver;  //class variable

	

	
	
	//constructor--initiate the driver	automtically
	 
	LoginPage(WebDriver driver){ //local variable
		this.driver=driver;  //class variable =local variable
	}
//locators--locate the element
	By emailText_locator=By.xpath("//input[@id='ap_email']");
	By pwText_locator=By.xpath("//*[@id=\"ap_password\"]");
	By loginText_locator=By.className("/a-button-input");
	
	//Action methods
	
	public void setUserName(String user) {
		driver.findElement(emailText_locator).sendKeys(user);
	}
	public void setPw(String pw) {
		driver.findElement(pwText_locator).sendKeys(pw);
	}
	public void clicklogin() {
		driver.findElement(emailText_locator).click();
	}
	*/
	
	
}
